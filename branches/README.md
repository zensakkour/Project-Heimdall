# Branch Worktrees

Keep all future branch worktrees inside this folder to avoid mixing with other projects.

Example:

```powershell
git worktree add branches/feat-my-change -b feat/my-change master
```
